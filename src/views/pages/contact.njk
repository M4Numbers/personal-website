{% extends "base_layouts/homepage_base.njk" %}

{% import "macros/content.njk" as c %}

{% block banner_icon %}
    {{ c.fa_banner(top_page.fa_type, top_page.fa_choice) }}
{% endblock %}

{% block content %}
    <div class="container">

        {% for option in content.options %}
            {% if ((loop.index0 % 3) == 0) %}
                <div class="row mb-5">
            {% endif %}

            <div class="col-lg-4">
                {{ c.contact_option(option.contact_method, option.fa_style, option.fa_icon, option.contact_link) }}
            </div>

            {% if ((loop.index0 % 3) == 2) or (loop.last) %}
                </div>
            {% endif %}
        {% else %}
            <div class="row">
                <div class="col-lg-12">
                    <p>
                        There is nothing here at the moment. That usually means that there will be at some point in
                        the future, but I promise nothing.
                    </p>
                </div>
            </div>
        {% endfor %}

        <p>
            If your preferred method of communication was not mentioned, then too bad. If you want to talk to
            me through something like Steam, I'd prefer you to give me a heads up via e-mail or Twitter first,
            thanks.
        </p>
    </div>

    <hr class="mb-4" />
{% endblock %}
